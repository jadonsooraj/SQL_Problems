--196. Delete Duplicate Emails

--Table:
1. Person: id, email

--Conditions:
1. Write a solution to delete all duplicate emails, keeping only one unique email with the smallest id.

--OUTPUT: delete the duplicate record

--CODE:
DELETE p1
FROM person p1
JOIN person p2
WHERE p1.email=p2.email and p1.id>p2.id